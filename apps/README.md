# Apps Overview

This folder contains all deployable applications in the monorepo.

- `web` - Main Sky Style product (Next.js App Router)
- `docs` - VitePress documentation site
- `api` - Future standalone API service scaffold

Keeping each app isolated makes independent deploys easier while preserving one shared repository and Git history.
